const config = {
  google: 'UA-xxxxxxxxx-1',

  // Set it to false if you put next's export files in a subfolder like github pages
  // default folder name is your package name in package.json
  subfolder: false,
};

module.exports = config;
